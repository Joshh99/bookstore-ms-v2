apiVersion: v1
kind: Service
metadata:
  name: book-command-service
  namespace: bookstore-ns
spec:
  selector:
    app: book-command-service
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000