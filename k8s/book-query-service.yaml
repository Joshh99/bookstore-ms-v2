apiVersion: v1
kind: Service
metadata:
  name: book-query-service
  namespace: bookstore-ns
spec:
  selector:
    app: book-query-service
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000